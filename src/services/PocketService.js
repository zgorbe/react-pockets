class PocketService {
    addAction(pocket, amount, direction) {
    }
    addMovement(sourcePocket, destinationPocket, amount) {

    }
}

const pocketService = new PocketService();
export default pocketService;